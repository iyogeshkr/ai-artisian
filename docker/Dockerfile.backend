FROM node:20-alpine
WORKDIR /app
# Copy the entire repository so backend can delegate to functions/ during migration
COPY . .
# Install delegated functions runtime dependencies.
RUN npm --prefix functions ci --omit=dev
ENV NODE_ENV=production
EXPOSE 8000
CMD ["node", "backend/server.js"]
