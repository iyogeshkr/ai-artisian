{
  "name": "ai-artisan-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,hosting",
    "serve:local": "node src/server.js",
    "lint": "node --check src/index.js && node --check src/app.js && node --check src/server.js",
    "deploy": "firebase deploy --only functions,hosting"
  },
  "dependencies": {
    "@clerk/express": "^2.1.15",
    "@supabase/supabase-js": "^2.104.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.1.0",
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.0.1"
  }
}
